

```
cd existing_repo
git remote add origin https://gitlab.com/m.burmistrov/linux/bash-scripts.git
git branch -M main
git push -uf origin main
```



grep-file-with-regular-expression
#Do regular search by phrase inside of HTML file, clean from rest of the file text and keep search result only.